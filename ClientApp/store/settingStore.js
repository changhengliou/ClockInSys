const initState = {

}
export const actionCreators = {}
export const reducer = (state = initState, action) => {
    switch (action.type) {
        case '':
        default:
            return state;
    }
}